// Using the same colors array, use .filter() to create a new array that contains only colors with length > 4. Log it.
// Create an object called phone with these properties:
// brand: "OnePlus",
// model: "13R",
// price: 42000,
// is5G: true
// Then log: My phone is ${phone.brand} ${phone.model} and costs ₹${phone.price}

// Add a method inside the phone object called getInfo that returns:
// "${this.brand} ${this.model} - 5G: ${this.is5G ? 'Yes' : 'No'}"
// Call it and log the result.

// Using destructuring, extract brand and price from the phone object into variables, then log:
// Brand = ____ , Price = ____

// Create this array: const nums = [10, 25, 7, 42, 18]
// Use spread operator to create a new array that contains all nums + three more numbers: 100, 200, 300 at the end.

// (Mini challenge) Write a function describeFruit (arrow or normal — your choice) that takes a fruit name and its price, and returns a sentence like:
// "A mango costs ₹120 per kg."
// Call it three times with different fruits.
// Convert this if-else into a ternary (one line): JavaScriptlet

// Create this array:
// const colors = ["red", "blue", "green", "yellow", "purple"]
// Use .map() to create a new array where every color becomes uppercase. Log the new array.

const colors = ["red", "blue", "green", "yellow", "purple"];
